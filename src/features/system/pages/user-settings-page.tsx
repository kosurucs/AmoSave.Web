export function UserSettingsPage() {
  return (
    <section className="page-card">
      <h2 className="section-title">User Setting</h2>
      <p className="helper">Manage profile preferences, account defaults, and trading personalization options.</p>
    </section>
  );
}
